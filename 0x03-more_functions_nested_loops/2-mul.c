#include "holberton.h"

/**
  * mul - Takes two integers and multiplies them
  * @a: the first number taken to be multiplied
  * @b: the second number taken to be multiplied
  * Return: the value of the multiplication
  */

int mul(int a, int b)
{
	int sum;

	sum = a * b;
	return (sum);
}
